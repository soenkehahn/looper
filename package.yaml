name: loopnaut

source-dirs: src
dependencies:
  - base

executables:
  loopnaut:
    main: loopnaut-exec.hs
    c-sources:
      - src/foo.c
    extra-libraries:
      - jack
    dependencies: []

  generate:
    main: Generate.hs
    dependencies:
      - hsndfile
      - hsndfile-vector
      - random
      - vector

tests:
  spec:
    main: Spec.hs
    source-dirs:
      - test
    c-sources:
      - src/foo.c
    extra-libraries:
      - jack
    dependencies:
      - bytestring
      - directory
      - filepath
      - hsndfile
      - hsndfile-vector
      - hspec
      - mockery
      - vector
