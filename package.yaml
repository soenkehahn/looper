name: loopnaut

source-dirs: src
dependencies:
  - base

executables:
  loopnaut:
    main: Loopnaut.hs
    ghc-options: -threaded -O2
    dependencies:
      - SFML
      - getopt-generics

  generate:
    main: Generate.hs
    dependencies:
      - hsndfile
      - hsndfile-vector
      - vector
      - random
